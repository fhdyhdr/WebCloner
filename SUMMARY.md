## Objective
- **Round #18 — perfect cut juga untuk vite/nextjs**: user konfirmasi static/index.html (cut) BERANIMASI ✓ tapi vite/nextjs saat dirun masih tanpa animasi (masih varian CSS murni). Fix: ketiganya kini cut serupa — vite `index.html` = wrapper iframe `/full.html#cp-cut=…` + `public/full.html` (staticHtml) + `public/snippet.html`; nextjs `app/page.jsx` = komponen wrapper iframe sama + `public/full.html` + `public/snippet.html`. **GOTCHA KRITIS: middleware cloneProxy di vite.config mem-proxy semua path tak-dikenal ke situs sumber** → /full.html diproxy ke Framer → 404 shell 260KB; fix = LOCAL list dinamis: `"/full.html","/snippet.html"` ditambahkan saat snipped&&cut. Next aman: rewrite array = afterFiles, public menang. Verifikasi RUN NYATA `npm install && npx vite --port 5199`: ROOT 200 iframe ✓, FULL 200 =1.614.579 byte lokal ✓, SNIP 200 ✓; Playwright end-to-end: **section top:0 EXACT**, jangkar terkoreksi otomatis 14182 (hint usang 14697 — bukti anchor relatif bekerja), wheel held, video asli 3/6 hidup, 0 pageerror.
- **Round #17 — PERFECT CUT dengan JS asli (permintaan eksplisit user)**: user menolak pendekatan capture: "harusnya jangan capture, harusnya potong sempurna dan tetap sertakan function javascript/library cdn". SOLUSI: ZIP section kini = **embed cut** — `static/index.html` berupa iframe tepat seukuran section yang memuat `static/full/index.html` (KLONE HALAMAN PENUH dgn SEMUA script asli, 15 tag script utk shinta) → semua animasi/library/CDN jalan persis situs aslinya; `static/snippet.html` = varian murni CSS (lama); vite/nextjs tak berubah. Mekanisme pinning (CP_CUT_BOOT di staticHtml, hash-gated `#cp-cut=<path>&cp-y=<y>`): path = child-index dari <body> ke elemen (dihitung saat pick di preview DOM), boot RETRY tiap 200ms s/d 12s karena **saat parse struktur body ≠ post-hidrasi Framer** (boot inline pertama GAGAL diam-diam - pelajaran!), lalu jangkar RELATIF KE ELEMEN (bukan Y absolut - layout buka-mandiri bergeser ratusan px dr pick-time; terukur -515px) + re-anchor pasca load+1.5s + guard re-pin 250ms + blokir wheel/touch capture-phase. Hasil verifikasi file:// : SECTION top=**0 EXACT**, wheel tak menggeser, video asli berputar (3-4/6), 0 pageerror. Regresi preview hijau (PROCESS cpPk:10, WORKS cpI:3 running4 scrub, marquee ul+cp-y RUNNING).
- **Round #16 — runway dihapus + tombol replay**: keluhan user (1) "saat clone section selalu ada section lagi di bawahnya, hapus saja" → itu bukan section melainkan **CP_RUNWAY `<div style="height:200vh">`** yang dulu ditambahkan supaya animasi scroll-link punya ruang saat dibuka mandiri. DIHAPUS total dari snippetHtml/snippetViteHtml/snippetNextPage (export-zip.ts). Konsekuensi disadari & benar: cp-s beku di 0% saat mandiri = tampilan natural (reveal-type memang sudah dibuang); saat diembed di halaman panjang, scrub jalan normal via view-timeline host. Verifikasi zip proses baru: 200vh=0, cp-pk=40 ✓. (2) "animasi lagi lagi belum ada" → akar persepsi: entrance cuma main SEKALI ±1.6s saat potongan dibuat; kalau dilihat setelahnya tampak mati. Fix UX: tombol **"putar ulang animasi"** di bar pilihan (index.tsx: state snippetNonce di deps effect blob → URL blob baru = semua CSS animation restart; auto-switch ke preview potongan).
- **Round #15 — marquee element-pick FINAL FIXED (hover-pause)**: keluhan "running/marquee tidak ada" akar masalahnya = **ticker Framer melambat ~10× saat di-hover** (-10px/s → -1px/s, terverifikasi probe-hover.mjs), dan kursor user selalu menempel di titik klik → `expandToMovingAncestor` mengukur drift saat track nyaris berhenti → tak pernah lolos ambang. Fix: sebelum sampling, injeksi sementara `<style>html,body{pointer-events:none!important}</style>` (150ms) → :hover clear + handler JS menerima mouseleave asli dari browser → track balik full speed; sampling 2 interval ×600ms ambang 1.5px; style dibuang di finally. Trick yang SAMA dipasang di awal `captureTimeAnimations` (speed marquee salah ukur bila kursor masih hover). Verifikasi end-to-end klik chip ticker shinta (mode elemen): badge=**ul**, snippet 22 elemen ul:true lis:5 cp-y:1, MARQUEE RUNNING -20→-45px @10px/s penuh ✓. Regresi hijau semua: PROCESS cpPk:10+scrub ✓, WORKS cpI:3 running:4 ✓, GSAP spin ✓, video clickplay+cpI:7 ✓, regression.mjs OK tanpa console error ✓.
- Perbaiki webcloner (`D:\dev\project\webclone`) agar ZIP element/section identik dengan web asli. **AKAR MASALAH "TETAP TIDAK ADA ANIMASI" (#11→#13) DITEMUKAN & FIXED**: probe semua section shinta via fitur preview potongan menunjukkan section "Our process" menghasilkan snippet BENAR-BENAR STATIS (83 elemen, cpI:0, TANPA `<style id="cp-anim">` sama sekali) — animasi entrance Framer sudah SELESAI main sebelum user sempat mem-pick (sudah lepas dari getAnimations), jadi capture menemukan NOL animasi. Section yang kebetulan masih punya animasi berjalan saat diklik (works/hero/video) selamat — itulah kenapa tes saya selalu lolos sementara user terus melihat mati. **SOLUSI (prestate)**: buildPreviewHtml kini menyuntikkan `CP_PRESTATE_SCRIPT` yang merekam FIRST-SEEN computed opacity/transform/filter tiap elemen ke `window.__cpPre` (WeakMap, MutationObserver utk node hydration); saat pick, `applyAnimStyles` pass baru mendiff first-seen vs settled dan mensintesiskan reveal `@keyframes cp-pk<i>` + `.cp-pk<i>` (0.9s, stagger 70ms max 700ms; skip elemen yang sudah ber-class cp-a/e/s/w/t/y/h/g; threshold opacity>0.02 atau translate>24px). Tag `<style id="cp-anim">` kini SELALU ada (unconditional) di snippetHtml/Vite/Next. **Verifikasi**: PROCESS dari statis total → 10/10 entrance RUNNING saat preview dibuka (t=450ms), selesai staggered ~2s ✓; WORKS tak berubah (cpI:3, running:4) ✓; regresi gsapsite GSAP hidup + spin ✓, video shinta cpI:7+clickplay ✓.
- Fitur ronde #12 tetap: setelah pick sukses iframe otomatis menampilkan potongan via blob URL + toggle "lihat halaman penuh"/"preview potongan".

## Important Details
- **Arsitektur preview (DeviceFrame, index.tsx ~line 1421-1450)**: `contentH` diukur via POLLING `setInterval(measure,700)` 25s — ResizeObserver TIDAK bisa (border-box `<html>` tetap seukuran viewport meski scrollHeight tumbuh). `safeH=clamp(contentH,200,16000)` (runtime situs liar bisa menggembungkan scrollHeight ke 2^25px cap Chrome). `frameCssH=round(min(safeH, 0.7×innerHeight/scale))`; wrapper height = `frameCssH×scale`. TANPA sticky/mirror/wheel-delegation. **GOTCHA: jangan tambahkan mirror scroll app→iframe atau wrapper setinggi halaman** — keduanya sudah dicoba dan gagal (cpI:0 / ruang kosong raksasa; lihat Objective).
- **Gotcha pengujian**: check-video2 dkk membaca folder `unz-*` — HARUS re-`Expand-Archive` zip terbaru sebelum check (pernah bikin cpI:0 palsu). check-height.mjs butuh srv.cjs port 9999 HIDUP. Semua `pick-*.mjs` punya `viewportPoint` terpatch: `f.scrollIntoView({block:'start'})` + tunggu 500ms + clamp y [5,985]. Badge = `span.font-semibold`. cpI count flaky 4-7 antar run (reveal terpicu saat sweep = balapan timing) — >0 dan cp-go>0 yang penting.
- Preview clone: `http://localhost:8080/?preview=<id>` (server.ts `getPreviewHtml`, raw HTML clone — SAME-ORIGIN dengan UI). Alur uji: isi placeholder → "Clone sekarang" → tunggu frame `?preview=` (20-40s flaky; NOFRAME = retry pick).
- **Sniping (2 mode)** — `installPicker(doc, mode, onPick, onCancel)` di `index.tsx`, **langsung attach capture listener ke `frame.contentDocument` (same-origin), TIDAK lagi inject `<script>`**. `pickElement` = e.target ber-id/class, jika tidak → ancestor ber-label paling dalam (max 4); `pickSection` = ancestor terdekat match `section, header, footer, article, main, aside, nav, [data-section], [class*=section], [id*=section]` (max 10) → fallback pickElement. Shift+klik = perluas ke ancestor ber-label terbesar (max 6). Esc batal. Hover outline via inline style (backup `data-cp-old`), cursor crosshair via `<style id="clone-picker-style">`.
- **Penting (bug sebelumnya)**: `t instanceof Element` di parent realm GAGAL untuk elemen iframe (realm berbeda) → gunakan `nodeType === 1`. Akar masalah lama: Vite/esbuild transform template literal `PICKER_SCRIPT` mengubah `\\s` → `\s` (3 char hilang) → `new Function` gagal "Unexpected string"; browser juga sempat menormalisasi `\\s`. Kini NOL injeksi string script.
- **Layout capture (feedback user)**: mode element → `captureHtml` clone elemen, merge inline style + computed LAYOUT_PROPS (`display,position,width,height,max/min, margins, paddings, boxSizing, overflow, zIndex, top/right/bottom/left, aspectRatio, objectFit, flex/grid container+item props, textAlign`; skip `auto`/`none`/`static`/`normal`/`visible`) → bungkus `<div style="display:flex/grid;...">` bila induk flex/grid (jaga posisi/centering). `cleanCapture` (kedua mode) buang artifact `data-cp-old` + `style=""` recursive.
- **KRITIS — kebab-case**: `getPropertyValue("flexDirection")` (camelCase) mengembalikan `""` untuk properti multi-kata di Chrome → flex-direction/margin/padding/box-sizing/gap/align-items dll. DROP senyap. Fix: `getPropertyValue(camelToKebab(p))`. Sebelum fix, capture card Framer kehilangan `flex-direction` → layout beda dari asli.
- **Akurasi round 2 (feedback "kurang akurat")**: (a) `text-wrap` masuk TYPO_PROPS — Framer memakai `text-wrap: balance` di heading; tanpa itu greedy wrapping memindah kata ke baris lain (terverifikasi: kata "scroll-stopping" pindah baris). (b) `copySvgDefs`: ikon `<use href="#id">` merujuk sprite `<svg>` di luar subtree (Framer: `div#svg-templates` di body) — defs yang hilang disalin ke container hidden di awal clone (juga menangkap `fill/clip-path/mask/filter="url(#id)"`); PENTING: harus dipanggil SETELAH walk paralel live/clone (inlineTree/applyAnimStyles) karena menyisipkan node menggeser pasangan node. (c) LAYOUT_PROPS + boxShadow, opacity (skip 1), border width/color per-sisi (skip bila semua width 0), borderStyle, verticalAlign; TYPO_PROPS + textShadow, -webkit-text-stroke width/color, textDecorationLine.
- **Glitch scroll saat pick (feedback user)**: sampling scroll animasi memindah scrollTop preview 9x — terlihat sebagai scroll aneh. Fix: saat capture, `html.clone-capturing body { visibility: hidden !important }` (class dipasang/dibuang di captureHtml, style di #clone-picker-style); computed styles tetap terbaca normal saat visibility hidden.
- **Verifikasi piksel (shinta section, canvas diff 12px-block)**: sebelum fix 849 blok beda (9.6%), sesudah 438 — dan 138 di antaranya adalah overlay tombol "buy" level halaman yang memang bukan anak section (exclude benar), sisanya ~300 blok bertekstur teks dengan computed style & box identik = noise anti-aliasing font. Struktur card identik node-per-node (probe paralel 11 node, termasuk `<use>` ikon yang tadinya 0x0 → kini sama).
- **Background ikut tercapture (feedback "warna berbeda")**: LAYOUT_PROPS + `backgroundColor, backgroundImage (hanya gradient, url() dilewati — proxy/dataURI rusak/bloat), color, borderRadius`. Plus `effectiveBackground(el)`: bila elemen/ancestor terpilih transparan, cari ancestor non-transparan TERDEKAT (Framer: section transparan di atas `<main>`/`<body>`) dan tempel `background-color` ke wrapper (atau root bila tanpa wrapper). Terverifikasi shinta: section → wrapper `background-color:rgb(244,244,243)` = computed `<main>` asli; card → `rgb(255,168,242)` = computed asli; transparan (`rgba(0,0,0,0)`) dilewati.
- **Typography inline (feedback "header kecil-kecil + berjarak")**: Framer mensize heading via `@container`/`@media` dari base kecil → di luar konteks container text jatuh ke ukuran base. Fix: `TYPO_PROPS` (`fontFamily, fontSize, fontWeight, fontStyle, lineHeight, letterSpacing, textTransform, whiteSpace`) di-inline pada elemen ber-teks (`needsTypo`: hasDirectText / class `framer-text` / tag h1-h6,p,span,a,li,dt,dd,blockquote,figcaption,button,label). Terverifikasi: H1 incredibles 122.4px = persis asli; H2 shinta 64px, kata-kata opacity 1 (bukan 0.001 pre-reveal).
- **Animasi ikut tercapture (feedback "tetap ada animasi, misal gsap/hover")**: `installPicker` kini melakukan 2 teknik → pure CSS:
  - **Hover**: simulasi enter/leave (mouse + pointer events) pada tiap node subtree, diff computed ANIM_PROPS, emit rule `#cp-root .cp-h<i>:hover { <delta> !important }` + `transition` (bila ada) di `<style id="cp-anim">` (disuntikkan ke head static/vite/next). Hanya prop visual (transform/translate/rotate/scale/opacity/backgroundColor/color/boxShadow/filter/outline/borderColor); elemen invisible baseline (group-hover dim, opacity<0.5) di-skip; hover-state yang menyembunyikan elemen juga di-skip. `!important` dipakai karena deep-inline menang atas rule biasa. Verifikasi: nav shinta baseline opacity 1 → hover `matrix(0.967,…)` + opacity 0.7 → lepas kembali.
  - **Scroll (GSAP/ScrollTrigger)**: sampling ANIM_PROPS di N=9 posisi scroll, elemen yang berubah → `@keyframes cp-k<i>` + `animation: … linear both; animation-timeline: scroll(root);`. Reveal entrance (opacity <0.5 di sampel) DIBUANG agar header tetap terlihat (piece pendek tak punya scroll room → kalau dipertahankan jadi beku tak terlihat). Verifikasi incredibles: `transform` 0 → translateY 421 saat scroll 2000.
  - Clone sekarang `#cp-root` (scoping rule); `CloneSelection` + `{ styles }`; snippetHtml/Vite/Next menyuntik `styles` ke head. Sebelum capture, `fireLeave(el)` menetralkan JS hover (Framer meredupkan link saudara saat satu di-hover) supaya base = "nothing hovered".
- **Deep-inline**: `inlineTree(live, clone)` menghitung computed style dari SUBTREE LIVE (detached clone tidak punya computed style!) lalu menerapkan 1:1 ke clone (parallel walk). Framer/lainnya meletakkan layout di CSS class (bukan inline) yang bergantung pada ancestor yang TIDAK ikut tercapture → deep-inline membuat potongan self-contained & pixel-faithful. Section mode juga memakai jalur ini (bukan outerHTML polos) → width/height/padding section ikut di-inline (fix "100% lebar").
- `buildZip(result, targets, selection?)` → snippet (static/vite/nextjs): markup terpilih + styleBlocks/linkTags + `<base href>`, TANPA script situs.
- **formatHtml(html)**: tokenizer inline/void/verbatim, block stack, `findTagEnd` (quoted `>`), `normalizeTag`, text ws handling. Preview TIDAK diformat.
- Playwright-core di `C:\Users\trias\AppData\Local\Temp\opencode\pwdrive`. Script: `snipe2-test.mjs` (menunggu loader hidden), `snipe-shinta.mjs` (element card + section, scrollIntoView + click), `snipe-render.mjs`, `probe-computed.mjs`, `shinta-inspect.mjs`, `debug-ui*.mjs`, `diff-script.mjs`, `bisect-script.mjs`, `pick-incredibles.mjs`/`pick-shinta-nav.mjs` (element/section pick + download), `render-incred.mjs`/`render-nav-hover.mjs`/`render-hover-root.mjs` (verifikasi render + hover + scroll), `diff-shinta.mjs` + `diffhost.html` (pixel-diff original vs captured via canvas; butuh browser terpisah dengan `--allow-file-access-from-files`; scroll asli pakai window.scrollTo + verifikasi rectTop≈0 — scrollIntoView tidak reliable di situs Framer), `probe-h2.mjs`/`probe-h2struct.mjs` (font + struktur heading), `probe-cardcmp.mjs` (bandingkan subtree card paralel), `probe-diffpts.mjs`.
- Restart dev: kill port 8080 → `Start-Process powershell ... npm run dev *> devserver.log` → tunggu ~25-30s. `npx tsc --noEmit` bersih.
- **Preferensi user**: preview 60fps+ (JANGAN cap 30fps); `ecoPreviewPatch()` freeze saat hidden/out-of-view; preview-only, tidak masuk ZIP.
- Environment MITM/network buruk: `player.vimeo.com` unreachable → 502 via proxy → NATIVE/expected, bukan bug cloner.



## Objective
- Perbaiki webcloner (`D:\dev\project\webclone`) agar hasil clone element/section akurat & animasinya hidup. Semua feedback user (shinta.framer.media, khusus section): background beda, text header kecil/jarak, animasi hover+scroll hilang, glitch scroll saat pick, foto card harusnya rounded, foto melingkar kehilangan rotasi, posisi text header tidak rapi - SEMUA SUDAH FIX.

## Important Details
- Preview clone: `http://localhost:8080/?preview=<id>` (SAME-ORIGIN). Restart dev: kill port 8080 -> `Start-Process powershell -ArgumentList "-NoProfile","-Command","Set-Location 'D:\dev\project\webclone'; npm run dev *> devserver.log" -WindowStyle Hidden` -> tunggu ~22s. `npx tsc --noEmit` HARUS dari `D:\dev\project\webclone`.
- **Glitch scroll fix**: saat capture `html.clone-capturing body { visibility:hidden !important }` (rule di `#clone-picker-style`; class dipasang/dibuang di captureHtml try/finally). Guard `capturing=true` DITAHAN sampai SETELAH cloneNode - setelah scroll sweep Chrome men-synthesize mouse events di bawah kursor parkir yang meng-aplikasikan ulang outline biru picker ke dalam clone.
- **Accuracy props**: LAYOUT_PROPS += boxShadow, opacity(skip "1"), border per-side width/color, borderStyle, verticalAlign; TYPO_PROPS += textShadow, textWrap, WebkitTextStrokeWidth/Color, textDecorationLine. Guard border: `(p.endsWith("Color") || p === "borderStyle") && !hasBorder` - JANGAN startsWith("border") (mematikan borderRadius pada elemen tanpa border -> foto rounded hilang).
- **Viewport-unit heights TIDAK di-bake**: iframe preview app di-scale via transform (panel 700px -> innerHeight ~935) sehingga 100vh terbaca 935px; baking computed px membuat section beku 935px & header geser 17px. Fix: `vpUnits()` kumpulkan selector rule yang set height ber-unit vh/vw; `sizedByViewport(el)` skip inline height utk elemen tsb -> stylesheet hasil copy (height:100vh) tetap apply, responsif.
- **captureTimeAnimations**: rotasi JS-driven (rAF/GSAP, invisible ke getAnimations) dideteksi dgn sampling matrix 2x (320ms), disintesis jadi `@keyframes cp-tk<i>` + `.cp-t<i> { animation: ... linear infinite }`; set `animated` di-skip dari hover/scroll capture.
- **Marker pairing mutation-proof**: `data-cp-i` dipasang SEBELUM sampling; clone dipasangkan via marker (`pairs: Map<number,HTMLElement>`); node baru hasil mutasi mid-capture = skip; `inlineTreeMarked`/`applyAnimStyles` baca isConnected guard; marker dibuang dari clone + live; cleanCapture strip sisa.
- **Hover sweep perf**: `scopeOf(idx)` = self + ancestor depth 5 + children; hanya HOVER_PROPS utk scope (dulu O(N^2) bikin section besar timeout -> download full-page clone).
- **copySvgDefs(clone)**: sprite `<use href="#id">` merujuk defs di luar subtree -> disalin ke `[data-cp-sprites]` (display:none) di root clone. WAJIB SETELAH pairing walk.
- **Urutan captureHtml**: fireLeave -> flatNodes+mark -> capturing=true/hide -> time/hover/scroll sampling -> restore(hovered) -> DOM-stability wait (poll flatNodes count stabil 2x200ms, max 14 iter) -> tick -> cloneNode -> pairs -> inlineTreeMarked -> applyAnimStyles -> copySvgDefs -> finally unhide+capturing=false.
- **Fakta shinta**: proxy preview TIDAK punya wrapper `.ssr-variant` (Framer variant pre-selected server-side) - layout-transparent, TIDAK perlu fix. Ring "Circular loop" berputar = sudut screenshot pasti beda antar waktu (expected diff noise). Diff proses section 438 blok = overlay tombol buy level halaman (exclude benar) + AA font.
- Playwright gotchas: clip butuh width/height; file:// image taint canvas kecuali launch flag `--allow-file-access-from-files`; scrollIntoView tak reliable di Framer (pakai window.scrollTo + verifikasi); badge pick = `span.font-semibold`; ID numerik = getElementById bukan querySelector.


## Objective
- Webcloner (`D:\dev\project\webclone`): user melapor ZIP element/section "berantakan, tanpa animasi, layout salah" (shinta.framer.media, buka static/index.html via file://) sementara ZIP full-page sempurna. TIGA AKAR MASALAH DITEMUKAN & DIPERBAIKI, semua terverifikasi.

## Important Details
- Restart dev: kill port 8080 -> `Start-Process powershell -ArgumentList "-NoProfile","-Command","Set-Location 'D:\dev\project\webclone'; npm run dev *> devserver.log" -WindowStyle Hidden` -> tunggu ~22s. `npx tsc --noEmit` dari root repo.
- **AKAR #1 - pickSection depth**: walk ancestor cuma 10 level; DOM Framer bersarang puluhan level -> di hero shinta fallback ke fragmen div kecil (`framer-gc7mty-container`) TANPA img/animasi = "berantakan + tidak ada animasi". Fix: limit 10 -> 80. Badge pick harus dicek: section.framer-15cgbio (bukan div.*).
- **AKAR #2 - konteks ancestor hilang**: rule tinggi section ada di CSS tersalin `.framer-V7F88 .framer-15cgbio { height:100vh }` tapi class `framer-V7F88` (wrapper luar) tidak ikut ter-capture -> height tak match -> section jatuh ke min-height 800px (real site: 900=100vh). Fix: `contextClasses` (class wrapper struktural antara el dan body, EXCLUDE html/body) ditaruh di wrapper INNER `display:contents` di dalam #cp-root -> descendant rule match lagi, tapi rule yang styling class konteks ITU sendiri tak punya box untuk dilukis (cp-root tetap netral; versi pertama menempelkan class langsung ke cp-root menyebabkan geser horizontal -120px dari padding/margin rule konteks).
- **AKAR #3 - vh beku di ruang iframe 935px**: preview iframe di-scale sehingga innerHeight 935 vs window user ~900. vpUnits LAMA hanya skip `height`; properti vertikal lain (top/margin-y/padding-y/min-height) ter-bake px ruang-935 -> konten bergeser saat dibuka di window nyata. Fix: generalisasi `vpUnitRules()` per-properti (VP_VERT set: height/min-height/max-height/top/bottom/margin-top/bottom/padding-top/bottom); inline style ber-unit viewport dipertahankan VERBATIM ("height:100vh" tetap 100vh - dulu di-skip total sehingga jatuh ke min-height stylesheet).
- **Materialisasi gate += display & visibility** (selain transform/opacity/filter/clip-path/animation-*/transition-*): strip marquee "NEW PROJECT!" ter-bake display:none dari rule base ber-gate -> kini computed display dimaterialisasi.
- **audit-real.mjs SITE CLONE_HTML**: audit BARU yang membandingkan clone vs SITUS ASLI DI WINDOW NYATA 1440x900 (bukan lewat iframe preview 935) - ground truth sesuai pengalaman user. Hasil hero shinta: secH 900=900 EXACT, 8/9 elemen delta 0, 1 miss = false-miss whitespace key teks (pola dikenal). 9 keyframes cp-s semua RUNNING, 0 pageerror.
- Regresi penuh lolos setelah semua fix: studiodialect hero 0 offender, shinta More works 0, Our process 13/14 (+1 false-miss whitespace H2), diff-shinta pixel 438 baseline exact, artifacts bersih.
- Struktur export sekarang SELALU: `<div id="cp-root" style="[flex/grid ctx][bg]"><div class="[contextClasses]" style="display:contents;">CLONE</div></div>`.
- Gotcha: `npx tsc` HARUS dari webclone dir (di pwdrive keluar "not the tsc command"); nested Promise evaluate GC -> split dua evaluate.

## Work State
### Completed
- **Cut untuk vite/nextjs (#18)**: wrapper + public/full.html + public/snippet.html; cloneProxy LOCAL dinamis (fix 404 proxy); verifikasi run nyata vite: top:0 exact, jangkar auto-koreksi, wheel held, video hidup.
- **Perfect cut embed (#17)**: static/index.html = iframe cut → full/index.html (JS asli lengkap); snippet.html = varian CSS; pin element-relative + retry boot + wheel-block. Verifikasi file:// top=0 exact, video jalan, 0 error.
- **Runway dihapus + replay button (#16)**: CP_RUNWAY hilang dari semua snippet builder (zip: 200vh=0, cp-pk tetap 40); tombol "putar ulang animasi" restart blob via nonce. Regresi hijau.
- **Hover-pause ticker + ekspansi track (#15)**: pointer-events:none trick di expandToMovingAncestor + captureTimeAnimations; chip pick → badge ul, cp-y, RUNNING penuh. Semua regresi hijau.
- **Prestate entrance recovery (#13)**: CP_PRESTATE_SCRIPT di buildPreviewHtml + pass delta di applyAnimStyles + tag cp-anim unconditional. PROCESS 0→10 animasi terverifikasi RUNNING saat load; semua regresi hijau (works/gsapsite/video). tsc OK, dev server restarted.
- **Marquee element-pick expansion (#14)**: `expandToMovingAncestor` (index.tsx, setelah resolve): sampling transform rantai ancestor 6 level; pilih ancestor steady-drift dengan ≥3 children & ukuran wajar; hanya mode elemen. Mencegah pick teks dalam marquee menghasilkan fragmen tanpa track loop. (v2 #15: SELALU cari moyang bergerak — tanpa early-return el — karena teks dalam track ikut bergerak sehingga cek "el sendiri bergerak" tak berguna.)
- **Preview potongan setelah pick (permintaan #12)**: index.tsx — Index state `showSnippetView`+`snippetUrl` (blob dari `snippetHtml(result, formatHtml(selection.html), selection.styles)`, revoke on cleanup); auto-switch saat pick sukses, mati saat mulai pick baru/kloning ulang; DeviceFrame prop `srcOverride` (src iframe, deps measure+picker, picker di-skip saat override); `snippetHtml` kini diekspor; tombol toggle di badge pilihan. Uji live shinta works: blob ✓ animasi hidup ✓ scrub ✓ toggle ✓. tsc OK.
- Round animasi asli di ekspor section FINAL: cpRuntimeHarness + judge konservatif (MutationObserver style + delta WAAPI; sampling-motion palsu dibuang). 3 jalur hijau: works-shinta sintesis hidup, gsapsite GSAP asli hidup, lenis fallback OK. Video shinta cpI+clickplay ✓.
- Round #9 tinggi preview FINAL: frame = min(konten, band 0.7vh) — pendek exact-fit, panjang capped + scroll internal.
- Round progress overlay selesai: overlay progress bar per-fase di dalam iframe; onClick double-capture guard.
- Round sebelumnya: WAAPI-infinite capture (cp-wk; computedOffset + persentase fix); marquee drift cp-y + smooth wheel Lenis-like; video click-to-play + intro-replay; currentSrc bake; entrance replay; Next.js layout.jsx; group-hover cp-g; runway 200vh; formatHtml whitespace-preserving.

### Active
- Perfect cut (#17) selesai & terverifikasi. Menunggu user mencoba: ZIP section baru → buka static/index.html = potongan ber-JS penuh. Preview potongan (blob) tetap varian CSS murni dgn tombol replay.

## Next Move
- Bila user ingin preview potongan juga menampilkan versi JS penuh (bukan CSS murni), bisa dibuatkan tab kedua di DeviceFrame (blob embed serupa). Bila ada animasi masih hilang pada situs lain, minta URL + elemen.

### Gotcha ronde ini
- **Middleware cloneProxy di vite.config mem-proxy SEMUA path tak-dikenal ke situs sumber** — file public sendiri (full.html) ikut terproxy (404 shell 260KB dari Framer). Setiap file public baru pada build cut WAJIB masuk daftar LOCAL.
- Next.js: rewrite berbentuk ARRAY = afterFiles → file public/ menang tanpa perlu pengecualian.
- Invoke-WebRequest/curl di lingkungan ini bisa lewat proxy sistem: gunakan `curl --noproxy "*"` + hostname `localhost` (vite bind IPv6 ::1; 127.0.0.1 gagal).
- **Boot inline berjalan SAAT PARSE: struktur body belum final** (Framer menghidrasi/menata ulang setelahnya) → walk path gagal diam-diam. SELALU retry-until-found utk locator lintas-hidrasi.
- **Y absolut pick-time ≠ waktu buka** (font/gambar late-load menggeser layout, terukur -515px; run vite: 14697→14182) → jangkar wajib relatif elemen + re-anchor pasca load.
- file:// iframe = origin opaque; Playwright tetap bisa evaluate via CDP (frames()).

## Next Move
- Lapor ke user (Bahasa Indonesia): fitur preview potongan sudah jadi — setelah pick sukses iframe otomatis menampilkan potongan persis seperti isi ZIP (animasi & scrub terverifikasi hidup di preview shinta works), ada toggle balik ke halaman penuh. Minta user: Ctrl+Shift+R → kloning ulang → pick → lihat preview. Bila animasi TIDAK muncul juga di preview potongan → bug capture spesifik elemen itu (minta URL + nama bagiannya); bila MUNCUL di preview tapi mati di static/index.html hasil download → masalah lingkungan buka file di sisi user (minta cara membukanya).

## Relevant Files
- `D:\dev\project\webclone\src\lib\export-zip.ts` - cpRuntimeHarness(r) (~line 433, setelah linkTags): filter script klasik + normalisasi src + harness boot/judge; dipanggil di snippetHtml/snippetViteHtml/snippetNextPage; CP_VIDEO_SCRIPT wheel guard lenis; README zip snipped branch.
- `D:\dev\project\webclone\src\routes\index.tsx` - DeviceFrame: contentH polling + frameCssH fit/cap band (TANPA sticky/mirror) + prop `srcOverride` (blob snippet); Index: state showSnippetView/snippetUrl + effect blob build + toggle badge; installPicker: showCaptureProgress() overlay + onClick rewrite; captureHtml(el, onProgress?) prog() per fase; captureWAAPIInfinite; captureTimeAnimations marquee drift cp-yk.
- `C:\Users\trias\AppData\Local\Temp\opencode\pwdrive\` - gsapsite/index.html di waapisite (fixture GSAP CDN klasik: scrub timeline + spin infinite), pick-generic2.mjs (pick section generik arg URL+zipname), check-gsap-section.mjs (sukses-path: gsapGlobal/animDisabled/spin moving), check-fallback.mjs (fallback-path: markup utuh + anim re-enabled), check-harness.cjs, check-height.mjs / check-height-shinta.mjs (cap 700px), probe-anim-early.mjs, pick-shinta-hero/video/ticker.mjs, check-video2.mjs, audit-real.mjs, srv.cjs port 9999 (WAJIB hidup utk fixture lokal).
- Zip aktif: gsap-section.zip/unz-gsap (GSAP asli HIDUP), lenis-section.zip/unz-lenis (fallback OK), shinta-video.zip/unz-video (cpI:7), shinta-hero.zip/unz-shinta-h (9/9), shinta-ticker.zip/unz-ticker, waapi-test.zip/unz-waapi, studio-hero.zip/unz-studio.

## Objective
- Webcloner (`D:\dev\project\webclone`): user melapor ZIP element/section "berantakan, tanpa animasi, layout salah" sementara ZIP full-page sempurna. AKAR STRUKTURAL: full zip (`staticHtml`) menyertakan `scriptTags(r)` (script asli situs → React/GSAP hydrate → semua animasi JS jalan) DAN `<body${r.bodyAttrs}>` (class body seperti `.loaded` ikut); section zip (`snippetHtml`) hanya markup+CSS tanpa script — kualitas bergantung penuh pada sintesis CSS kita.

## Important Details
- Restart dev: kill port 8080 -> `Start-Process powershell -ArgumentList "-NoProfile","-Command","Set-Location 'D:\dev\project\webclone'; npm run dev *> devserver.log" -WindowStyle Hidden` -> tunggu ~22s. `npx tsc --noEmit` dari root repo.
- **Gate materialization v2**: gate classes = html/body classes + SEMUA class ancestor chain dari picked subtree (`gateExtra`, direset tiap captureHtml). Materialisasi DIBATASI ke properti animation-state SAJA: transform/translate/rotate/scale/opacity/filter/clip-path/visibility + animation-*/transition-*. ALASAN: di Framer hampir semua selector membawa class wrapper ancestor - saat SEMUA prop rule dimaterialisasi, layout dinamis ikut beku (regresi nyata: H2/A "See More Works" shinta geser dy=17px; fixed oleh filter ini -> 0 offender lagi).
- **Repro griflan.com** (full zip vs section zip, section.mb-150): section zip JUSTRU LEBIH AKURAT dari full zip di region section (pixel diff 2.5% vs 13.8%); motion check: live 0.2%, keduanya 0% (section memang statis). Tidak berhasil repro keluhan "berantakan total" user dengan griflan/shinta/studiodialect - perlu URL spesifik dari user.
- **Perbedaan struktural full vs section** (export-zip.ts): staticHtml = styleBlocks+linkTags+headTags+bodyAttrs+scriptTags+SCROLL_JS(+sw.js utk framework); snippetHtml = styleBlocks+linkTags+cp-anim styles saja, body polos. snippetVite/Next juga tanpa script. README.zip menjelaskan static/index.html = yang dibuka langsung.
- audit-pos final: studiodialect hero 0 offenders (animation-name revealElement masih ter-materialisasi ✓), shinta More works 0 offenders, Our process 13/14 (+1 false-miss whitespace), diff-shinta 438 baseline.
- Playwright gotchas tambahan: model tidak bisa baca screenshot PNG (pakai canvas block-diff kuantitatif); motion check = 2 screenshot 1.4s apart dibandingkan.

## Work State
### Completed
- Gate materialization diperluas (ancestor chain) + difilter (animation-state only); regresi 17px shinta ditemukan & diperbaiki; studiodialect + shinta verified 0 offenders.
- Investigasi keluhan user: akar struktural full-vs-section didokumentasikan; repro griflan menunjukkan section zip sehat.

### Blocked
- Butuh URL situs + section spesifik dari user untuk repro kasus "berantakan" mereka (tidak repro di griflan/shinta/studiodialect). Juga perlu tahu: varian mana yang dibuka (static/vite/nextjs) dan via file:// atau http server.

## Next Move
- Tanya user: (1) URL situs yang di-test, (2) section/elemen yang dipilih, (3) file yang dibuka dari ZIP (static/index.html?), (4) cara membukanya (double-click / di-server). Setelah dapat repro -> fix spesifik.

## Relevant Files
- `D:\dev\project\webclone\src\routes\index.tsx` - installPicker: gateExtra + gateSelectorRules (filter animation-state props, ~line 210-270), inlineTreeMarked materialization, stability wait v2, captureHtml (gateExtra reset + ancestor collection di awal).
- `D:\dev\project\webclone\src\lib\export-zip.ts` - staticHtml (full: scripts+bodyAttrs) vs snippetHtml/snippetViteHtml/snippetNextPage (section: tanpa script), buildZip (root index.html = snippet utk selection).
- `C:\Users\trias\AppData\Local\Temp\opencode\pwdrive\` - repro-griflan.mjs (full+section zip satu sesi), cmp-griflan.mjs, diff-griflan.mjs, motion-griflan.mjs (motion+region diff), audit-pos.mjs, pick-studio-hero.mjs, pick-shinta-process.mjs, pick-shinta-works.mjs, diff-shinta.mjs, probe-settle.mjs, probe-gate2.mjs.
- Zip aktif: griflan-full.zip/unz-griflan-full, griflan-sec.zip/unz-griflan-sec, studio-hero.zip/unz-studio, shinta-works.zip/unz-works, shinta-process.zip/unz-shinta-p.

## Objective
- Webcloner (`D:\dev\project\webclone`): keluhan user "beberapa element posisinya kurang rapi" — AKAR MASALAH DITEMUKAN & FIXED. Dua penyebab element bergeser/scale salah di clone: (1) DOM-stability wait lama hanya cek node count + inline style = buta terhadap CSS transition class-gated; (2) rule CSS ber-gate class di LUAR subtree (mis. `.loaded .x { transform: scale(1) }`, `.loaded` di `<body>`) tidak match di export → state pre-load (scale 1.1 / opacity 0) BEKU selamanya.

## Important Details
- Restart dev: kill port 8080 -> `Start-Process powershell -ArgumentList "-NoProfile","-Command","Set-Location 'D:\dev\project\webclone'; npm run dev *> devserver.log" -WindowStyle Hidden` -> tunggu ~22s. `npx tsc --noEmit` dari root repo.
- **Stability wait v2** (sebelum cloneNode): signature = node count + COMPUTED transform+opacity per node (exclude `time.animated`) + jumlah animasi finite aktif (`doc.getAnimations()`, filter target dalam subtree, skip iterations===Infinity, playState running; CATATAN: lib.dom TS tidak punya "pending" di AnimationPlayState). Stabil 2 round x 250ms, max 18 round (~4.5s). Alasan computed (bukan inline): transisi CSS yang di-trigger class TIDAK menulis inline style.
- **Gate materialization** (`gateSelectorRules()` + ekstensi `inlineTreeMarked`): kumpulkan style rules yang selector-nya mengandung class dari `documentElement.className + body.className` (gate classes) DAN punya descendant combinator; untuk tiap elemen pair yang `matches()` selector tsb, materialisasikan computed value SEMUA properti rule itu sebagai inline (TERMASUK default seperti transform:none/opacity:1 - justru itu yang harus mengalahkan rule base pre-load di export). Cache per capture. Contoh studiodialect: base `.element-reveal-item{transform:translate3d(0,-.5em,0) scale(1.1);opacity:0}` vs gated `.loaded .element-reveal.loaded .element-reveal-item{opacity:1;animation:revealElementFromBottom...,elementScalePulse...}` - tanpa materialization, export pakai branch base = P anak ter-scale 1.1 (317px vs 288px, dx=-14 dw=+29).
- **Efek samping positif**: materialisasi animation-* longhands membuat clone ME-REPLAY entrance animation situs (delay+fill forwards ada di CSS tersalin) lalu settle ke final state - terverifikasi probe-settle: t=2s box [562,376,317] (mid-entrance), t=4.5s [576,385,288] = EXACT live.
- **audit-pos.mjs SITE CLONE_HTML SECTION_SEL**: audit posisi otomatis live vs clone (img/h1-h6/p/a/button, box relatif section, scroll disamakan via spacer y+200, skip time-animated >2px/400ms, laporkan delta >3px). Clone side WAJIB wait 5.5s sebelum ukur (entrance delay). SECTION_SEL = data-framer-name ATAU querySelector. Bug lama sudah fix: arg CP_SEC (dulu hardcode), nested Promise GC -> dua evaluate, polling 40s + DIAG.
- **Hasil audit final**: studiodialect section.hero 0 offenders (dulu 1); shinta "More works" 0 offenders; shinta "Our process" (section beneran, badge section.framer-thgd3i) 13/14 + 1 false-miss whitespace H2; diff-shinta pixel 438 = baseline exact; artifacts semua zip bersih (0 leftover mark, 0 outline biru, cp-root ada).
- **Gotcha snipe-shinta.mjs**: step "Pilih section"-nya FLAKY - klik bisa tertutup overlay/nav dropdown sehingga yang ter-pick DIV kartu (badge div.framer-W2Q9M) bukan section; zip shinta-section.zip dari run itu SALAH target. Untuk regresi Our process pakai `pick-shinta-process.mjs` (scrollIntoView + click center section by framer-name, badge harus section.framer-thgd3i) -> unz-shinta-p.
- Marker pairing data-cp-i mutation-proof; capturing guard ditahan sampai snapshot; copySvgDefs setelah pairing; vpUnits/sizedByViewport (vh tidak di-bake); view-timeline cp-s; entrance cp-a/cp-e; absolutizeLinks.
- Fonts embedded data URI - bukan penyebab misalignment. Proxy preview tanpa wrapper .ssr-variant - layout-transparent.

## Work State
### Completed
- Element posisi kurang rapi FIXED + verified: stability wait v2 (computed sig + activeAnims) + gate materialization; studiodialect hero audit 0 offenders, settle exact [576,385,288].
- Regresi lengkap lolos: More works 0, Our process 13/14 (+1 false-miss whitespace), diff-shinta 438 baseline, artifacts bersih semua zip.
- tsc bersih; dev server running dengan kode baru.

## Next Move
- Laporan ke user (Bahasa Indonesia): penyebab "posisi kurang rapi" = state pre-load animasi reveal ber-gate class luar yang beku di clone; kini dimaterialisasi + entrance di-replay. Kalau masih ada kasus, minta URL + section spesifik.

## Relevant Files
- `D:\dev\project\webclone\src\routes\index.tsx` - installPicker: gateSelectorRules (BARU, ~line 210), inlineTreeMarked + materialization gated props (BARU, ~line 260-285), stability wait v2 di captureHtml (computed sig + activeAnims), captureEntranceAnimations, captureTimeAnimations, captureScrollAnimations (view-progress), applyAnimStyles (cp-a/cp-h/cp-t/cp-s + --cpv), absolutizeLinks, sizedByViewport/vpUnits, marker pairing.
- `C:\Users\trias\AppData\Local\Temp\opencode\pwdrive\` - audit-pos.mjs (wait 5.5s clone side), pick-studio-hero.mjs, pick-shinta-process.mjs (BARU, section Our process reliable), pick-shinta-works.mjs, diff-shinta.mjs (kini pakai unz-shinta-p + wait 5.5s), check-artifacts.cjs, probe-settle.mjs (bukti replay+settle), probe-baked.mjs, probe-gate.mjs/probe-gate2.mjs (diagnosis gate), probe-parallax.mjs, check-interactivity.mjs, final-check.cjs, hover-check.cjs, snipe-shinta.mjs (FLAKY section step), pick-incredibles.mjs.
- Zip/extract aktif: studio-hero.zip/unz-studio (studiodialect hero, kode TERBARU), shinta-works.zip/unz-works, shinta-process.zip/unz-shinta-p (Our process), shinta-element.zip/shinta-section.zip (snipe, section zip salah target run terakhir), incredibles-section.zip/unz-incred.

## Objective
- Webcloner (`D:\dev\project\webclone`): clone element/section harus sama persis dgn web asli - layout, animasi hidup & smooth, dan interaktif (bisa diklik). Semua feedback user (shinta.framer.media) sudah ditangani.

## Important Details
- Restart dev: kill port 8080 -> `Start-Process powershell -ArgumentList "-NoProfile","-Command","Set-Location 'D:\dev\project\webclone'; npm run dev *> devserver.log" -WindowStyle Hidden` -> tunggu ~22s. `npx tsc --noEmit` dari root repo.
- **Scroll animasi: view-timeline (BUKAN scroll(root))**: progress scroll(root) tergantung tinggi halaman - di export standalone halaman hanya setinggi section -> animasi MATI. Sekarang `captureScrollAnimations` men-tag tiap sample dgn view-progress root `(y-(docY-vh))/(vh+H)` clamped 0..1; keyframes multi-stop pakai persentase tsb (dedupe tail clamped); rule `#cp-root { view-timeline-name: --cpv }` + `.cp-s<i> { animation: cp-k<i> linear both; animation-timeline: --cpv }`. Return type `Map<number, {rows, prog}>`.
- **Entrance animation capture** (`captureEntranceAnimations`, jalan PERTAMA sebelum time/hover/scroll): finite WAAPI (iterations===1, endTime finite, bukan CSSTransition) -> rekam getKeyframes() jadi `@keyframes cp-e<i>` + `.cp-a<i> { animation: ... ms both }`, lalu `a.finish()` supaya base inline/hover/scroll membaca FINAL state (bukan mid-entrance). Animation yang sudah selesai + fill:none sudah lepas dari getAnimations - tak tertangkap (limitasi). CSS-declared animations TIDAK perlu di-capture: stylesheet asli disalin utuh di styleBlocks -> replay sendiri (contoh elementScalePulse studiodialect ada di ZIP).
- **Link absolutize** (`absolutizeLinks(clone)`): href relatif (`./projects`) 404 di export -> resolve ke origin asli via canonical/og:url (fallback: skip). Skip `#anchor` & absolute URL.
- **Interaktivitas = native CSS, bukan JS situs**: hover replay cp-h (:hover + transition), scroll cp-s (view-timeline), spin cp-t (keyframes infinite), entrance cp-a/cp-e + CSS situs, link <a href> absolut. JS runtime situs (React/GSAP Framer) TIDAK bisa disertakan utk section/element - hydration butuh DOM penuh.
- Viewport-unit height tidak di-bake (vpUnits/sizedByViewport) - iframe app scaled melaporkan vh lebih besar (935 vs 900).
- Marker pairing data-cp-i mutation-proof; capturing guard ditahan sampai snapshot (outline biru tidak bocor); copySvgDefs setelah pairing; DOM-stability wait sebelum cloneNode.
- Fonts embedded sebagai data URI woff2 (45 @font-face di shinta) - font BUKAN penyebab misalignment.
- Verifikasi shinta More works: secH 900, h2RelTop 413-415 (dinamis oleh scroll anim state), scroll transform BERUBAH saat discroll (changed:true), hover transform berubah (true), link https://shinta.framer.media/projects, spin cp-tk2 running, 0 pageerror.
- Regresi: proses section diff 438 (baseline noise: overlay buy + AA font); studiodialect elementScalePulse ada + 0 artifact; incredibles 17 cp-k + view-timeline + 0 leftover mark.

## Work State
### Completed
- Animasi mati/tidak smooth FIXED: scroll anim kini main di export (view-timeline remap) + entrance WAAPI terekam + CSS situs replay native.
- Link tidak bisa diklik FIXED: href di-absolutize ke situs asli.
- Header alignment: exact match live site (415 @1440x900); sisa delta 1-2px = posisi animasi scroll yang memang dinamis per scroll position.
- Regresi bersih semua (shinta proses/studiodialect/incredibles).

## Next Move
- Laporan ke user. Kalau masih ada keluhan alignment/animasi, minta URL situs + section spesifik utk repro (per-situs beda mekanisme animasi).

## Relevant Files
- `D:\dev\project\webclone\src\routes\index.tsx` - installPicker: captureEntranceAnimations (BARU), captureTimeAnimations, captureScrollAnimations (view-progress remap), applyAnimStyles(cp-a/cp-h/cp-t/cp-s + rule --cpv), absolutizeLinks (BARU), sizedByViewport/vpUnits, marker pairing, captureHtml pipeline.
- `C:\Users\trias\AppData\Local\Temp\opencode\pwdrive\` - pick-shinta-works.mjs, check-interactivity.mjs, check-incred-anims.mjs, final-check.cjs, hover-check.cjs, check-artifacts.cjs, diff-shinta.mjs, snipe-shinta.mjs, snipe2-test.mjs, pick-incredibles.mjs, measure-preview.mjs, watch-wrapper.mjs.
